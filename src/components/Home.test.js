'use strict';

describe('<Home />', function () {
  describe('renders', function () {
    it('<IconSelector />', function () {

    });

    it('<StickerModeToggle />', function () {

    });

    it('provided <ActiveIconView />', function () {

    });
  });

  it('stays up to date with sticker mode', function () {
    // mock handlers passed down and change their values and see
    // if they reflect in state
  });

  it('stays up to date with the selected icon', function () {
    // mock handlers passed down and change their values and see
    // if they reflect in state
  });
});
