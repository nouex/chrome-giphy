import { Button } from "react-bootstrap"

const React = require('react');
const ReloadBtn = ({onClick}) => <div className="px-4 py-2"><Button bsStyle="primary" onClick={onClick}>New</Button></div>

export default ReloadBtn
